- content_for :page_title, 'Your Repos'

= render 'settings'

- if display_onboarding?
  = render 'onboarding'

%section.organization-list{ 'organization-list' => '' }
= react_component('ReposContainer', authenticity_token: form_authenticity_token, has_private_access: current_user.has_access_to_private_repos?)

=# render 'preload_template', template: 'organization_list'
